---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<!doctype html>
<html lang="fr">
	<head>
		<BaseHead 
			title="Fassia Résidence Nongo - Conakry"
			description="Hôtel moderne à Nongo, Conakry avec piscine, restaurant et salles de conférence"
		/>
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@300;400;500;600&family=Lato:wght@300;400;500&display=swap" rel="stylesheet">
		<style>
			* {
				margin: 0;
				padding: 0;
				box-sizing: border-box;
			}

			:root {
				--gold: #C5A572;
				--dark: #1a1a1a;
				--off-white: #fafafa;
			}

			html {
				scroll-behavior: smooth;
			}

			body {
				font-family: 'Lato', sans-serif;
				color: #2a2a2a;
				background: white;
				overflow-x: hidden;
			}

			h1, h2, h3 {
				font-family: 'Playfair Display', serif;
				font-weight: 400;
			}

			/* SCROLL ANIMATIONS */
			.fade-in {
				opacity: 0;
				transform: translateY(60px);
				transition: opacity 0.8s cubic-bezier(0.4, 0, 0.2, 1), 
				            transform 0.8s cubic-bezier(0.4, 0, 0.2, 1);
			}

			.fade-in.visible {
				opacity: 1;
				transform: translateY(0);
			}

			.fade-in-scale {
				opacity: 0;
				transform: scale(0.95);
				transition: opacity 0.8s ease-out, transform 0.8s ease-out;
			}

			.fade-in-scale.visible {
				opacity: 1;
				transform: scale(1);
			}

			.slide-in-left {
				opacity: 0;
				transform: translateX(-80px);
				transition: opacity 0.8s cubic-bezier(0.4, 0, 0.2, 1), 
				            transform 0.8s cubic-bezier(0.4, 0, 0.2, 1);
			}

			.slide-in-left.visible {
				opacity: 1;
				transform: translateX(0);
			}

			.slide-in-right {
				opacity: 0;
				transform: translateX(80px);
				transition: opacity 0.8s cubic-bezier(0.4, 0, 0.2, 1), 
				            transform 0.8s cubic-bezier(0.4, 0, 0.2, 1);
			}

			.slide-in-right.visible {
				opacity: 1;
				transform: translateX(0);
			}

			/* Hero with Slideshow */
			.hero {
				height: 90vh;
				position: relative;
				overflow: hidden;
			}

			.hero-slideshow {
				position: absolute;
				width: 100%;
				height: 100%;
			}

			.hero-slide {
				position: absolute;
				width: 100%;
				height: 100%;
				opacity: 0;
				transition: opacity 2s ease-in-out, transform 20s ease-out;
				background-size: cover;
				background-position: center;
				transform: scale(1);
			}

			.hero-slide.active {
				opacity: 1;
				transform: scale(1.05);
			}

			/* Ken Burns effect on slides */
			.hero-slide:nth-child(1) { 
				background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
			}
			.hero-slide:nth-child(2) { 
				background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
			}
			.hero-slide:nth-child(3) { 
				background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
			}

			.hero-overlay {
				position: absolute;
				width: 100%;
				height: 100%;
				background: linear-gradient(to bottom, rgba(0,0,0,0.3), rgba(0,0,0,0.6));
				z-index: 1;
			}

			.hero-content {
				position: relative;
				z-index: 2;
				height: 100%;
				display: flex;
				flex-direction: column;
				justify-content: center;
				align-items: center;
				text-align: center;
				color: white;
				padding: 2rem;
			}

			.hero h1 {
				font-size: clamp(3rem, 8vw, 6rem);
				margin-bottom: 1rem;
				font-weight: 300;
				letter-spacing: 3px;
				animation: heroFadeIn 1.2s cubic-bezier(0.4, 0, 0.2, 1);
				text-shadow: 0 4px 30px rgba(0,0,0,0.5);
			}

			.hero-location {
				font-size: clamp(1rem, 2.5vw, 1.5rem);
				letter-spacing: 6px;
				text-transform: uppercase;
				opacity: 0.95;
				animation: heroFadeIn 1.4s cubic-bezier(0.4, 0, 0.2, 1) 0.2s both;
			}

			.scroll-hint {
				position: absolute;
				bottom: 3rem;
				left: 50%;
				transform: translateX(-50%);
				color: white;
				font-size: 0.85rem;
				letter-spacing: 2px;
				text-transform: uppercase;
				animation: bounce 2s infinite, heroFadeIn 1.6s ease-out 0.4s both;
				z-index: 3;
				cursor: pointer;
			}

			.scroll-hint::after {
				content: '↓';
				display: block;
				margin-top: 0.5rem;
				font-size: 1.5rem;
			}

			/* Progress Bar */
			.scroll-progress {
				position: fixed;
				top: 0;
				left: 0;
				width: 0%;
				height: 4px;
				background: var(--gold);
				z-index: 9999;
				transition: width 0.2s ease;
			}

			/* Intro Section with Parallax */
			.intro {
				padding: 10rem 2rem;
				text-align: center;
				max-width: 900px;
				margin: 0 auto;
				position: relative;
			}

			.intro::before {
				content: '';
				position: absolute;
				top: 50%;
				left: 50%;
				transform: translate(-50%, -50%);
				width: 150%;
				height: 150%;
				background: radial-gradient(circle, var(--gold) 0%, transparent 70%);
				opacity: 0.03;
				pointer-events: none;
			}

			.intro h2 {
				font-size: clamp(2.5rem, 5vw, 3.5rem);
				margin-bottom: 2rem;
				color: var(--dark);
			}

			.intro p {
				font-size: 1.2rem;
				line-height: 2.2;
				color: #6a6a6a;
			}

			.divider {
				width: 100px;
				height: 3px;
				background: var(--gold);
				margin: 3rem auto;
				position: relative;
				overflow: hidden;
			}

			.divider::after {
				content: '';
				position: absolute;
				top: 0;
				left: -100%;
				width: 50%;
				height: 100%;
				background: linear-gradient(90deg, transparent, rgba(255,255,255,0.8), transparent);
				animation: shimmer 2.5s infinite;
			}

			/* Photo Gallery with Advanced Animations */
			.gallery-section {
				padding: 8rem 2rem;
				background: var(--off-white);
				position: relative;
				overflow: hidden;
			}

			.gallery-section h2 {
				font-size: clamp(2.5rem, 5vw, 3.5rem);
				text-align: center;
				margin-bottom: 4rem;
				color: var(--dark);
			}

			.gallery-grid {
				display: grid;
				grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
				gap: 2.5rem;
				max-width: 1400px;
				margin: 0 auto;
			}

			.gallery-item {
				position: relative;
				aspect-ratio: 4/3;
				background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
				border-radius: 12px;
				overflow: hidden;
				cursor: pointer;
				transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
				box-shadow: 0 10px 30px rgba(0,0,0,0.1);
			}

			.gallery-item::before {
				content: '';
				position: absolute;
				top: 0;
				left: 0;
				width: 100%;
				height: 100%;
				background: linear-gradient(45deg, transparent 30%, rgba(255,255,255,0.3) 50%, transparent 70%);
				transform: translateX(-100%);
				transition: transform 0.6s ease;
				z-index: 2;
			}

			.gallery-item:hover::before {
				transform: translateX(100%);
			}

			.gallery-item:hover {
				transform: translateY(-15px) scale(1.02);
				box-shadow: 0 25px 50px rgba(0,0,0,0.2);
			}

			.gallery-item img {
				width: 100%;
				height: 100%;
				object-fit: cover;
				transition: transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);
			}

			.gallery-item:hover img {
				transform: scale(1.15);
			}

			.gallery-label {
				position: absolute;
				bottom: 0;
				left: 0;
				right: 0;
				padding: 2rem;
				background: linear-gradient(to top, rgba(0,0,0,0.95), transparent);
				color: white;
				font-size: 1.3rem;
				font-family: 'Playfair Display', serif;
				transform: translateY(100%);
				transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1);
				z-index: 3;
			}

			.gallery-item:hover .gallery-label {
				transform: translateY(0);
			}

			/* Gradient backgrounds for each item */
			.gallery-item:nth-child(1) { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); }
			.gallery-item:nth-child(2) { background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%); }
			.gallery-item:nth-child(3) { background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%); }
			.gallery-item:nth-child(4) { background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%); }
			.gallery-item:nth-child(5) { background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 100%); }
			.gallery-item:nth-child(6) { background: linear-gradient(135deg, #fbc2eb 0%, #a6c1ee 100%); }
			.gallery-item:nth-child(7) { background: linear-gradient(135deg, #fdcbf1 0%, #e6dee9 100%); }
			.gallery-item:nth-child(8) { background: linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%); }
			.gallery-item:nth-child(9) { background: linear-gradient(135deg, #a1c4fd 0%, #c2e9fb 100%); }

			/* Features Section */
			.features {
				padding: 10rem 2rem;
				background: white;
				position: relative;
			}

			.features h2 {
				font-size: clamp(2.5rem, 5vw, 3.5rem);
				text-align: center;
				margin-bottom: 6rem;
				color: var(--dark);
			}

			.features-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
				gap: 3rem;
				max-width: 1200px;
				margin: 0 auto;
			}

			.feature-card {
				text-align: center;
				padding: 3rem 2rem;
				transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1);
				position: relative;
			}

			.feature-card::before {
				content: '';
				position: absolute;
				top: 50%;
				left: 50%;
				transform: translate(-50%, -50%);
				width: 0;
				height: 0;
				background: var(--off-white);
				border-radius: 50%;
				transition: width 0.5s ease, height 0.5s ease;
				z-index: -1;
			}

			.feature-card:hover::before {
				width: 120%;
				height: 120%;
			}

			.feature-card:hover {
				transform: translateY(-10px);
			}

			.feature-icon {
				font-size: 4rem;
				margin-bottom: 1.5rem;
				display: block;
				transition: transform 0.4s ease;
			}

			.feature-card:hover .feature-icon {
				transform: scale(1.1) rotateY(360deg);
			}

			.feature-card h3 {
				font-size: 1.5rem;
				margin-bottom: 1rem;
				color: var(--dark);
				transition: color 0.3s ease;
			}

			.feature-card:hover h3 {
				color: var(--gold);
			}

			.feature-card p {
				color: #6a6a6a;
				line-height: 1.8;
			}

			/* Contact Section */
			.contact-section {
				padding: 10rem 2rem;
				background: var(--dark);
				color: white;
				text-align: center;
				position: relative;
				overflow: hidden;
			}

			.contact-section::before {
				content: '';
				position: absolute;
				top: 0;
				left: 0;
				width: 100%;
				height: 100%;
				background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1440 320"><path fill="rgba(197,165,114,0.1)" d="M0,96L48,112C96,128,192,160,288,160C384,160,480,128,576,112C672,96,768,96,864,112C960,128,1056,160,1152,160C1248,160,1344,128,1392,112L1440,96L1440,320L1392,320C1344,320,1248,320,1152,320C1056,320,960,320,864,320C768,320,672,320,576,320C480,320,384,320,288,320C192,320,96,320,48,320L0,320Z"></path></svg>') repeat-x;
				opacity: 0.5;
				animation: wave 20s linear infinite;
			}

			.contact-section h2 {
				font-size: clamp(2.5rem, 5vw, 3.5rem);
				margin-bottom: 3rem;
				position: relative;
				z-index: 2;
			}

			.contact-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
				gap: 4rem;
				max-width: 1000px;
				margin: 0 auto 4rem;
				position: relative;
				z-index: 2;
			}

			.contact-item {
				transition: transform 0.3s ease;
			}

			.contact-item:hover {
				transform: translateY(-5px);
			}

			.contact-item h3 {
				font-size: 1.5rem;
				color: var(--gold);
				margin-bottom: 1rem;
			}

			.contact-item p {
				font-size: 1.1rem;
				line-height: 1.8;
				opacity: 0.9;
			}

			.phone-link {
				color: white;
				text-decoration: none;
				font-size: 2rem;
				font-weight: 500;
				display: block;
				margin: 1rem 0;
				transition: all 0.3s ease;
				position: relative;
			}

			.phone-link::after {
				content: '';
				position: absolute;
				bottom: -5px;
				left: 50%;
				transform: translateX(-50%);
				width: 0;
				height: 2px;
				background: var(--gold);
				transition: width 0.3s ease;
			}

			.phone-link:hover::after {
				width: 100%;
			}

			.phone-link:hover {
				color: var(--gold);
				transform: scale(1.05);
			}

			.social-link {
				display: inline-block;
				margin-top: 1.5rem;
				padding: 1.2rem 3rem;
				background: transparent;
				border: 2px solid white;
				color: white;
				text-decoration: none;
				text-transform: uppercase;
				letter-spacing: 2px;
				font-size: 0.9rem;
				transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
				position: relative;
				overflow: hidden;
			}

			.social-link::before {
				content: '';
				position: absolute;
				top: 50%;
				left: 50%;
				width: 0;
				height: 0;
				background: white;
				transform: translate(-50%, -50%);
				border-radius: 50%;
				transition: width 0.5s ease, height 0.5s ease;
				z-index: -1;
			}

			.social-link:hover::before {
				width: 300%;
				height: 300%;
			}

			.social-link:hover {
				color: var(--dark);
				border-color: white;
			}

			.back-button {
				display: inline-block;
				padding: 1.2rem 3rem;
				background: var(--gold);
				color: var(--dark);
				text-decoration: none;
				text-transform: uppercase;
				letter-spacing: 2px;
				font-size: 0.9rem;
				font-weight: 600;
				transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
				position: relative;
				z-index: 2;
			}

			.back-button:hover {
				background: #d4b88a;
				transform: translateY(-3px);
				box-shadow: 0 10px 30px rgba(197,165,114,0.4);
			}

			/* Animations */
			@keyframes heroFadeIn {
				from {
					opacity: 0;
					transform: translateY(40px);
				}
				to {
					opacity: 1;
					transform: translateY(0);
				}
			}

			@keyframes bounce {
				0%, 100% { transform: translate(-50%, 0); }
				50% { transform: translate(-50%, 10px); }
			}

			@keyframes shimmer {
				0% { left: -100%; }
				100% { left: 100%; }
			}

			@keyframes wave {
				0% { background-position-x: 0; }
				100% { background-position-x: 1440px; }
			}

			/* Responsive */
			@media (max-width: 768px) {
				.gallery-grid {
					grid-template-columns: 1fr;
					gap: 2rem;
				}

				.features-grid {
					grid-template-columns: 1fr;
				}

				.hero {
					height: 70vh;
				}
			}
		</style>
	</head>
	<body>
		<div class="scroll-progress"></div>
		<Header />

		<!-- Hero with Slideshow -->
		<section class="hero">
			<div class="hero-slideshow">
				<div class="hero-slide active"></div>
				<div class="hero-slide"></div>
				<div class="hero-slide"></div>
			</div>
			<div class="hero-overlay"></div>
			<div class="hero-content">
				<h1>Fassia Résidence Nongo</h1>
				<p class="hero-location">Conakry, Guinée</p>
			</div>
			<div class="scroll-hint" onclick="document.querySelector('.intro').scrollIntoView({behavior: 'smooth'})">
				Découvrir
			</div>
		</section>

		<!-- Intro -->
		<section class="intro fade-in">
			<h2>Bienvenue à Nongo</h2>
			<div class="divider"></div>
			<p>
				Situé dans le quartier dynamique de Nongo à Conakry, notre hôtel offre 
				un cadre moderne et confortable. Que vous soyez en voyage d'affaires ou 
				de loisirs, profitez de nos installations de qualité et de notre service 
				chaleureux dans un environnement raffiné.
			</p>
		</section>

		<!-- Photo Gallery -->
		<section class="gallery-section">
			<h2 class="fade-in">Nos Espaces</h2>
			<div class="gallery-grid">
				<div class="gallery-item fade-in-scale">
					<!-- <img src="/images/nongo/chambre1.jpg" alt="Chambre" /> -->
					<div class="gallery-label">Chambres Confortables</div>
				</div>
				<div class="gallery-item fade-in-scale">
					<!-- <img src="/images/nongo/suite.jpg" alt="Suite" /> -->
					<div class="gallery-label">Suites Premium</div>
				</div>
				<div class="gallery-item fade-in-scale">
					<!-- <img src="/images/nongo/piscine.jpg" alt="Piscine" /> -->
					<div class="gallery-label">Piscine</div>
				</div>
				<div class="gallery-item fade-in-scale">
					<!-- <img src="/images/nongo/poolside.jpg" alt="Espace piscine" /> -->
					<div class="gallery-label">Espace Détente</div>
				</div>
				<div class="gallery-item fade-in-scale">
					<!-- <img src="/images/nongo/restaurant.jpg" alt="Restaurant" /> -->
					<div class="gallery-label">Restaurant</div>
				</div>
				<div class="gallery-item fade-in-scale">
					<!-- <img src="/images/nongo/conference.jpg" alt="Salle de conférence" /> -->
					<div class="gallery-label">Salle de Conférence</div>
				</div>
				<div class="gallery-item fade-in-scale">
					<!-- <img src="/images/nongo/jardin.jpg" alt="Jardin" /> -->
					<div class="gallery-label">Jardins</div>
				</div>
				<div class="gallery-item fade-in-scale">
					<!-- <img src="/images/nongo/reception.jpg" alt="Réception" /> -->
					<div class="gallery-label">Salle de Réception</div>
				</div>
				<div class="gallery-item fade-in-scale">
					<!-- <img src="/images/nongo/bar.jpg" alt="Bar" /> -->
					<div class="gallery-label">Bar & Lounge</div>
				</div>
			</div>
		</section>

		<!-- Features -->
		<section class="features">
			<h2 class="fade-in">Nos Services</h2>
			<div class="features-grid">
				<div class="feature-card slide-in-left">
					<span class="feature-icon">🛏️</span>
					<h3>Chambres Modernes</h3>
					<p>Hébergements climatisés avec tout le confort moderne</p>
				</div>
				<div class="feature-card fade-in">
					<span class="feature-icon">🏊</span>
					<h3>Piscine</h3>
					<p>Profitez de notre piscine pour vous détendre</p>
				</div>
				<div class="feature-card slide-in-right">
					<span class="feature-icon">🍽️</span>
					<h3>Restaurant</h3>
					<p>Cuisine locale et internationale</p>
				</div>
				<div class="feature-card slide-in-left">
					<span class="feature-icon">💼</span>
					<h3>Salles de Conférence</h3>
					<p>Espaces équipés pour vos événements professionnels</p>
				</div>
				<div class="feature-card fade-in">
					<span class="feature-icon">📶</span>
					<h3>WiFi Gratuit</h3>
					<p>Internet haut débit dans tout l'établissement</p>
				</div>
				<div class="feature-card slide-in-right">
					<span class="feature-icon">🚗</span>
					<h3>Parking Sécurisé</h3>
					<p>Stationnement gratuit et sécurisé</p>
				</div>
			</div>
		</section>

		<!-- Contact -->
		<section class="contact-section">
			<h2 class="fade-in">Réservations</h2>
			<div class="contact-grid">
				<div class="contact-item fade-in-scale">
					<h3>Téléphone</h3>
					<a href="tel:+224XXXXXXXXX" class="phone-link">+224 XXX XXX XXX</a>
					<p>Appelez-nous pour réserver</p>
				</div>

				<div class="contact-item fade-in-scale">
					<h3>Adresse</h3>
					<p>
						Quartier Nongo<br>
						Conakry, Guinée<br>
						Afrique de l'Ouest
					</p>
				</div>

				<div class="contact-item fade-in-scale">
					<h3>Suivez-nous</h3>
					<a href="https://www.facebook.com/p/Fassia-Residence-Nongo-100063245107199/" 
					   target="_blank" 
					   class="social-link">
						Facebook
					</a>
				</div>
			</div>

			<a href="/" class="back-button">← Retour à l'accueil</a>
		</section>

		<Footer />

		<script>
			// Hero Slideshow with Ken Burns effect
			let currentSlide = 0;
			const slides = document.querySelectorAll('.hero-slide');
			
			function nextSlide() {
				slides[currentSlide].classList.remove('active');
				currentSlide = (currentSlide + 1) % slides.length;
				slides[currentSlide].classList.add('active');
			}
			
			setInterval(nextSlide, 6000);

			// Scroll Progress Bar
			window.addEventListener('scroll', () => {
				const winScroll = document.body.scrollTop || document.documentElement.scrollTop;
				const height = document.documentElement.scrollHeight - document.documentElement.clientHeight;
				const scrolled = (winScroll / height) * 100;
				document.querySelector('.scroll-progress').style.width = scrolled + '%';
			});

			// Intersection Observer for Scroll Animations
			const observerOptions = {
				threshold: 0.15,
				rootMargin: '0px 0px -100px 0px'
			};

			const observer = new IntersectionObserver((entries) => {
				entries.forEach((entry, index) => {
					if (entry.isIntersecting) {
						// Stagger animation for gallery items
						if (entry.target.classList.contains('gallery-item')) {
							const items = document.querySelectorAll('.gallery-item');
							const itemIndex = Array.from(items).indexOf(entry.target);
							setTimeout(() => {
								entry.target.classList.add('visible');
							}, itemIndex * 80);
						} else if (entry.target.classList.contains('feature-card')) {
							// Stagger for feature cards
							const cards = document.querySelectorAll('.feature-card');
							const cardIndex = Array.from(cards).indexOf(entry.target);
							setTimeout(() => {
								entry.target.classList.add('visible');
							}, cardIndex * 100);
						} else {
							entry.target.classList.add('visible');
						}
					}
				});
			}, observerOptions);

			// Observe all animated elements
			document.querySelectorAll('.fade-in, .fade-in-scale, .slide-in-left, .slide-in-right, .gallery-item, .feature-card').forEach(el => {
				observer.observe(el);
			});

			// Parallax effect on intro section
			window.addEventListener('scroll', () => {
				const scrolled = window.pageYOffset;
				const intro = document.querySelector('.intro');
				if (intro) {
					const introOffset = intro.offsetTop;
					const introHeight = intro.offsetHeight;
					if (scrolled > introOffset - window.innerHeight && scrolled < introOffset + introHeight) {
						const parallax = (scrolled - introOffset) * 0.3;
						intro.style.transform = `translateY(${parallax}px)`;
					}
				}
			});
		</script>
	</body>
</html>